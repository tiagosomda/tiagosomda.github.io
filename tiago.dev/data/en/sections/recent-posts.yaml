# section information
section:
  name: My most recent notes
  id: recent-posts
  enable: true
  weight: 30
  showOnNavbar: false
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required

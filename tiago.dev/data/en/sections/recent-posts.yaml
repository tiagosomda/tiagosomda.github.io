# section information
section:
  name: Recent Notes
  id: recent-posts
  enable: true
  weight: 20
  showOnNavbar: true
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
